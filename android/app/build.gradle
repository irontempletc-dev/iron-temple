// DO NOT EDIT THIS FILE! IT IS GENERATED BY CAPACITOR ENZYME.
// Instead, edit android/variables.gradle, and run "npx cap sync"
// To learn more, see: https://capacitorjs.com/docs/android/configuration

apply from: "../variables.gradle"
apply plugin: 'com.android.application'

android {
    namespace "com.irontemple.traininghub"
    compileSdkVersion rootProject.ext.compileSdkVersion
    defaultConfig {
        applicationId "com.irontemple.traininghub"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        aaptOptions {
             // For more information about this topic, see:
             // https://fd.5201961.xyz:443/https/developer.android.com/guide/topics/resources/providing-resources#BestMatch
            additionalParameters "--extra-packages", "com.irontemple.traininghub"
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    lintOptions {
        abortOnError false
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation project(':capacitor-android')
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
    implementation project(':capacitor-cordova-android-plugins')
}

if (enablePostInstallPatch) {
    apply from: '../../node_modules/patch-package/raw-postinstall.js'
}
